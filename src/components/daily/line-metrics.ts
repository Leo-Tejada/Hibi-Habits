/**
 * The editor's input and the highlight overlay behind it must agree to
 * the pixel, or the colours slide off the characters. Both take these
 * classes, and neither may set type styles of its own.
 */
export const LINE_TYPE = 'font-mono text-[15px] leading-7 tracking-normal'

export const LINE_PAD = 'px-3 py-1'
